/*
* Programa de prueba para verificar la correcta funcionalidad de todas las producciones sintácticas
* de mi lenguaje de programacion "Gecko".
*/
Begin
    // Declaraciones de variables con inicialización
    Integer entero = 42;
    Float decimal = 3.14;
    String texto = "Hola Mundo";
    Character letra = 'A';
    Boolean verdad = true;

    // Declaración sin asignación inicial
    Integer sinValor;
	
	sinValor = 1;
	
	sinValor++; 
	
	sinValor--; 

    // Operadores aritméticos
    Integer suma = 5 + 3;
    Integer resta = 10 - 2;
    Integer multiplicacion = 4 * 2;
    Integer division = 20 / 5;
    Integer modulo = 7 % 3;

    // Operadores de comparación y lógicos
    Boolean comparacion1 = (5 == 5);
    Boolean comparacion2 = (3 != 4);
    Boolean comparacion3 = (7 < 10);
    Boolean comparacion4 = (8 > 6);
    Boolean comparacion5 = (9 <= 9);
    Boolean comparacion6 = (10 >= 10);
    Boolean logico = (true && false) || !false;

	//Operadores ternarios
	Boolean ternario = (logico == false) ? false : true;

    // Estructuras de control If-ElseIf-Else
    if (entero == 42) {
        texto = "El valor es 42";
    } else if (entero < 42) {
        texto = "El valor es menor que 42";
    } else {
        texto = "El valor es mayor que 42";
    }

    // Ciclo For con inicialización, condición y paso
    for (Integer i = 0; i < 5; i++) {
        entero = entero + 1;
    }

    // Ciclo While
    while (verdad == true) {
        verdad = false;  // Cambio de condición para evitar un ciclo infinito
    }

	Integer contador;
    // Ciclo Do-While
    do {
        texto = texto + "!";
        contador = contador + 1;
    } while (contador < 5);

    // Switch con casos y valor predeterminado
    switch (entero) {
        case 10:
            texto = "Diez";
            break;
        case 20:
            texto = "Veinte";
            break;
        case 30:
            texto = "Treinta";
        	break;
        default:
            texto = "Otro valor";
			break;    
    } 
End